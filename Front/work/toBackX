#!/bin/bash

# atkuldi a feltoltott "oda" ahova kell

source base
source def

cd ${toBack}

declare fName

while true ; do

  for fName in $(find . -type f -printf "%f " ) ; 
  do
# count uname problem lang    
    ${cpMethod} ${fName} ${target}/${fName}
    if [ $? = 0 ] ; 
    then
      rm -f ${fName} 
    fi
  done
  sleep 2

done
